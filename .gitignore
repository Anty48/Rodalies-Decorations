# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run
run-data

# Files from Forge MDK
forge*changelog.txt

# Jars de dependencias de dev de Create (compileOnly via flatDir 'libs'); no se versionan.
libs

# Area de staging: cosas que el usuario deja para que Claude copie (exports de Blockbench,
# texturas fuente, capturas, jars de Create). Local, no se versiona. Ver CLAUDE.md.
things_for_claude
